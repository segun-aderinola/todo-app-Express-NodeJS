<!DOCTYPE html>
<html lang="en">
  <head>
    <meta charset="UTF-8" />
    <meta http-equiv="X-UA-Compatible" content="IE=edge" />
    <meta name="viewport" content="width=device-width, initial-scale=1.0" />
    <title>To Do List App</title>
    <link rel="stylesheet" href="css/styles.css" />
  </head>
  <body>
    <div class="box" id="heading">
      <h1><%= listHeader %></h1>
    </div>
    
    <div class="box">
       
      <% newItem.forEach(element => { %>
        <div class="item">
            <input type="checkbox">
      <p><%= element %></p>
    </div>
      <% }); %>

      <form class="item" action="/" method="POST">
        <input type="text" name="todo" placeholder="Enter Task to do" class="todo" required autocomplete="off"/>
  
        <button name="addList" type="submit" value=<%= listHeader %>>+</button>
      </form>
</div>
<div class="center">
    <a href="<%= gotoPage %>"><% if(gotoPage ===  "/"){ %> 
        Go to Home
    <% }
    else{ %>
       View Work List 
    <% } %>
    </a>
</div>
   <div class="footer">
        Designed by Segun James
   </div>
  </body>
</html>
